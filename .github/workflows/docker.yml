# Copyright (c) Meta Platforms, Inc. and affiliates.
# All rights reserved.
name: Docker Image CI

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - 'docker/**'
      - 'gcm/**'
      - 'pyproject.toml'
      - 'requirements.txt'
      - '.github/workflows/docker.yml'

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  GCM_IMAGE: ghcr.io/${{ github.repository_owner }}/gcm
  NPD_IMAGE: ghcr.io/${{ github.repository_owner }}/gcm-npd

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      matrix:
        include:
          - dcgm_version: "4.4.2-1"
            tag: dcgm4
          - dcgm_version: "3.3.7-1"
            tag: dcgm3
    steps:
      - uses: actions/checkout@v4

      # QEMU provides arm64 emulation for cross-building on amd64 runners.
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build GCM image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name == 'push' }}
          tags: ${{ env.GCM_IMAGE }}:${{ matrix.tag }}
          build-args: DCGM_VERSION=${{ matrix.dcgm_version }}
          cache-from: type=gha,scope=gcm-${{ matrix.tag }}
          cache-to: type=gha,mode=max,scope=gcm-${{ matrix.tag }}

      # On push: NPD's FROM resolves against the multi-arch GCM manifest
      # just pushed to GHCR, so buildx picks the right arch per target
      # platform automatically.
      - name: Build and push NPD-GCM image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.npd
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.NPD_IMAGE }}:${{ matrix.tag }}
          build-args: GCM_IMAGE=${{ env.GCM_IMAGE }}:${{ matrix.tag }}

      # On PR: GCM isn't pushed, so fall back to a single-arch (amd64)
      # local load for NPD validation. The main GCM build above still
      # validates both archs.
      - name: Load GCM image for NPD build (PR validation)
        if: github.event_name != 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64
          push: false
          load: true
          tags: gcm:${{ matrix.tag }}
          build-args: DCGM_VERSION=${{ matrix.dcgm_version }}
          cache-from: type=gha,scope=gcm-${{ matrix.tag }}

      - name: Build NPD-GCM image (PR validation)
        if: github.event_name != 'push'
        run: docker build -f docker/Dockerfile.npd --build-arg GCM_IMAGE=gcm:${{ matrix.tag }} -t ${{ env.NPD_IMAGE }}:${{ matrix.tag }} .

  helm-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.17.0

      - name: Lint Helm chart
        run: helm lint charts/gcm

      - name: Template Helm chart
        run: |
          helm template test-release charts/gcm \
            --set monitoring.sink=stdout \
            --set monitoring.cluster=test \
            --set healthChecks.cluster=test
