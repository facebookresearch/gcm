{{/*
Copyright (c) Meta Platforms, Inc. and affiliates.
All rights reserved.
*/}}
{{- if .Values.healthChecks.enabled -}}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "gcm.fullname" . }}-health-checks
  labels:
    {{- include "gcm.labels" . | nindent 4 }}
    app.kubernetes.io/component: health-checks
spec:
  schedule: {{ .Values.healthChecks.schedule | quote }}
  concurrencyPolicy: {{ .Values.healthChecks.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.healthChecks.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.healthChecks.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "gcm.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: health-checks
        spec:
          serviceAccountName: {{ include "gcm.serviceAccountName" . }}
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          restartPolicy: OnFailure
          containers:
            - name: health-checks
              image: {{ include "gcm.image" . }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              command:
                - health_checks
              args:
                {{- if .Values.healthChecks.sink }}
                - --sink
                - {{ .Values.healthChecks.sink | quote }}
                {{- end }}
                {{- if .Values.healthChecks.cluster }}
                - --cluster
                - {{ .Values.healthChecks.cluster | quote }}
                {{- end }}
                {{- range .Values.healthChecks.extraArgs }}
                - {{ . | quote }}
                {{- end }}
              envFrom:
                - configMapRef:
                    name: {{ include "gcm.fullname" . }}
              resources:
                {{- toYaml .Values.healthChecks.resources | nindent 16 }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir:
                sizeLimit: 64Mi
          {{- with .Values.healthChecks.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.healthChecks.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
